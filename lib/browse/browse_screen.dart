import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class BrowseScreen extends StatelessWidget {

  static String routName = "browse screen";
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
